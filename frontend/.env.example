# The frontend talks to the API via a relative /api path, which the Vite dev
# server proxies to the backend (see vite.config.js). No env vars needed for
# local development. This file is a placeholder for future deploy-time config.
