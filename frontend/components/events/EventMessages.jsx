"use client";

// Thread scoped to one event — GET/POST /api/events/{id}/messages. There is no
// standalone chat surface anywhere else in the app; this component is the only place messages render.
export default function EventMessages({ eventId }) {
  return null;
}
